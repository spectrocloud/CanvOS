#!/bin/bash
# Image repository for image tagging
IMAGE_REPOSITORY="ttl.sh"

# Environment Options to build
# (ubuntu-lts-20, ubuntu-lts-22, opensuse-leap)
OS_FLAVOR="ubuntu-lts-22"
K8S_FLAVOR="k3s"
K8S_VERSIONS="1.25.2,1.24.6"
USER_DATA_FILE=user-data.yaml
CONTENT_BUNDLE="content_bundle.tar"

# ISO Naming Options and image naming
CANVOS_ENV=demo
ISO_IMAGE_NAME=$CANVOS_ENV-installer

# Automatically push images to repository
PUSH_BUILD=false

#### Sample Push command to AWS s3.  Modify per requirements.
# PUSH_COMMAND="aws s3 cp $ISO_IMAGE_NAME.iso s3://image.iso