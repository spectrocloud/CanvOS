# OpenSCAP / STIG OVAL checks this exact path and filename (see ssg enable_dracut_fips_module).
# kairos-fips.conf already adds fips; this file satisfies the scanner without changing dracut behavior.
add_dracutmodules+=" fips "
