# Dracut configuration for Kairos STIG images
# Ensures required modules and drivers are included in initramfs

add_drivers+=" squashfs overlay loop "
add_dracutmodules+=" systemd dmsquash-live rootfs-block "
