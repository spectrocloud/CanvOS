#!/bin/bash


USERNAME=$1
PASSWORD=$2
BASE_IMAGE="${3:-rhel9-byoi-fips}"

# Build the container image
docker build --build-arg USERNAME="$USERNAME" --build-arg PASSWORD="$PASSWORD" -t "$BASE_IMAGE" -f Dockerfile.rhel9 . 