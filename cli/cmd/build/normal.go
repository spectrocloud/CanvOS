package build

import (
	"context"

	"specrocloud.com/canvos/internal"
)

// Demo is the workflow orchestrator for the demo mode
func Normal(ctx context.Context, config *internal.CliConfig, options *internal.OptionsMenu) error {

	// pVersion, err := prompts.Select("Select the Palette Edge Installer version", paletteVersions, paletteVersions[0], "A Palette Edge Installer version is required")
	// if err != nil {
	// 	log.Debug("err %s: ", err)
	// 	log.FatalCLI("error getting API key. Exiting")
	// }
	// userSelectedOptions.PaletteEdgeInstallerVersion = pVersion

	return nil
}
